#ifndef MAIN_H
#define MAIN_H

/* Standard library files imported */

#include <stdarg.h>
#include <unistd.h>
#include <stdio.h>

/* Protoypes of functions */

int _putchar(char z);
int _printf(const char *format, ...);
int _put_s(char *string);
int _put_int(int integer);
int  _my_puts(char *string);
void  _get_int(int num);

#endif /* MAIN_H */
